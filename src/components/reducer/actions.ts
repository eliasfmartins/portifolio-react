
import { Dispatch } from 'react';
import * as types from './types';

export const loadLandingPage = () => {
 return{ type: types.PROJECT_LADING_PAGE };
};

export const loadFilmePage = () => {
return{ type: types.PROJECT_FILME_PAGE };
};
